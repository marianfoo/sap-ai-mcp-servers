# Optional local token for GitHub API enrichment.
# Recommended: create a classic PAT with at least public repo read access.
# The script falls back to HTML scraping if no token is set.
GH_TOKEN=ghp_your_token_here
